<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="adminPortal.css" />
    <link rel="shortcut icon" href="./favicon.png" type="image/x-icon" />
    <form action="/" method="get" id="homeForm"></form>
    <script>
      (function tokenValidate() {
        console.log('tokenValidating');
        if (localStorage.getItem('token') == null) {
          console.log('invalid');
          document.getElementById('homeForm').submit();
        } else {
          console.log('valid');
        }
      })();
    </script>

    <link
      href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Data Entry System</title>
  </head>
  <body>
    <div>
      <div class="header ">
        <div style="display: flex; width: 50%;">
          <div class="heading">Data Entry System</div>
        </div>
        <form id="btn" action="/logout" method="get" class="logoutButton">
          <div class="admin" id="user-mode-name">Administrator</div>
          <button
            type="button"
            onclick="logout()"
            style="
              background: linear-gradient(to right, #0f0c29, #302b63, #24243e);
            "
            class="button-logout bg-blue-500 hover:bg-blue-400 text-white font-bold py-2 px-4 border-b-4 border-blue-700 hover:border-blue-500 rounded"
          >
            LOGOUT
          </button>
        </form>
      </div>

      <div class="bodyContainer">
        <div class="createCompany" onclick="(() => document.getElementById('createCompanyDivision').style.visibility = 'visible')()">
          <img src="./icons/White CAdd Btn.png" alt="Add Company" class="createCompanyImg">
          <button class="companyButton lg:text-2xl md:text-xl sm:text-sm pt-2" >Create New Company</button>
        </div>
        <div class="companyTable lg:text-2xl md:text-xl sm:text-base">
          <div id="employeeTableDivision" class="overflow-x-auto   ">
            <table class=" table_content ">
              <tr class="uppercase  table-heading-container lg:text-base md:text-base sm:text-sm" >
                <th>Company ID</th>
                <th >Company Name</th>
                <th>Root User</th>
                <th>TOTAL BODIES</th>
                <th>USED BODIES</th>
                <th>AVAILABLE BODIES</th>
                <th>Date</th>
                <th>Time</th>
              </tr>
              <%for(let i=0; i < companyDetail.length; i++){
                const company = companyDetail[i];%>
                <tr class="lg:text-xl md:text-lg sm:text-xs">
                  <%for(let j=0; j< Object.keys(company).length; j++){
                    const property = Object.keys(company)[j]%>
                    <td ><%= company[property] %></td>
                  <%}%>
                </tr>
              <%}%>
            </table>
          </div>
        </div>
      </div>

      <div id="createCompanyDivision" >
        <div class="createCompanyModalContent">
          <div id="createCompanyContentHolder" style="display:flex; flex-direction: column; align-items: center; justify-content: center;">
            <h1 class="md:text-xl font-bold leading-tight tracking-tight text-gray-900 lg:text-2xl sm:text-xs dark:text-white mb-10" style="color: black; ">
              Create Company Account
            </h1>
            <form class="space-y-4 md:space-y-6" id="companyCreateForm">
                <div >
                  <label f style="color: black;"or="email" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Enter Company Name:</label>
                  <input type="text" required name="companyName" id="companyName" class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="Company Name" >
                </div>
                <div >
                    <label style="color: black;" for="email" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Create Username:</label>
                    <input type="text" required name="rootUserName" id="rootUserName" class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="Enter Username" >
                </div>
                <div >
                    <label style="color: black;" for="email" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">E-mail:</label>
                    <input type="email" required name="rootUserEmail" id="rootUserEmail" class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="Enter E-mail address" >
                </div>
                <div >
                    <label style="color: black;" for="password" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Password:</label>
                    <input type="password" required name="rootPassword" id="rootPassword" placeholder="Password" class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" >
                </div>
                <div >
                    <label style="color: black;" for="confirm-password" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">Confirm password:</label>
                    <input type="password" required name="confirmPassword" id="confirmPassword" placeholder="Confirm Password" class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" >
                </div>
                <div class="buttonStyle lg:grid lg:grid-cols-2 lg:grid-rows-1 lg:gap-4 md:grid-rows-2 md:grid-cols-1 md:gap-3 sm:flex sm:grid-cols-1 sm:grid-rows-2 sm:row-gap-6 sm:justify-center sm:items-center ">
                  <button type="button" id="submitButton" onclick="validate(event)" class="2xl:h-15 lg:w-50 lg:h-15 sm:w-full md:h-15 md:w-full  text-white bg-primary-600 hover:bg-primary-700 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800" style="background-color: #2461e5;">Create</button>
                  <button type="button" id="submitButton" onclick="(() => document.getElementById('adminPortalForm').submit())()" class="2xl:h-15 lg:w-50 lg:h-15 sm:w-full md:h-15 md:w-full text-white bg-primary-600 hover:bg-primary-700 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800" style="background-color: #2461e5;"> Cancel</button>
                </div>
            </form>
          </div>
        </div>
      </div>

      <div id="createCompanySuccessDivision" style="border-radius: 10px;" >
        <div class="createCompanySuccessModalContent" style="display: flex; flex-direction: column; align-items: center; justify-content: center; border-radius: 10px;">
          <div id="createCompanySuccessContentHolder" style="display:flex; flex-direction: column; align-items: center; justify-content: center; gap: 2rem;">
            <p id="createCompanySuccessMessage" style="font-size: larger; font-weight: 500;" ></p>
            <button class="text-white bg-[#24292F] hover:bg-[#24292F]/90 focus:ring-4 focus:outline-none focus:ring-[#24292F]/50 font-medium rounded-lg 
            text-sm px-5 py-2.5 text-center inline-flex items-center dark:focus:ring-gray-500 dark:hover:bg-[#050708]/30 mr-2 mb-2" onclick="(()=> document.getElementById('adminPortalForm').submit())()">OK</button>
          </div>
        </div>
      </div>

      <form action="/adminPortal" id="adminPortalForm" method="post">
        <input type="hidden" name="token" value="" id="token">
      </form>

    <script>

      document.getElementById('token').value = localStorage.getItem('token')

      function validate(event){
        event.preventDefault();
        var createCompanyForm = document.getElementById('companyCreateForm')
        if (!createCompanyForm.checkValidity()) {
          if (createCompanyForm.reportValidity) {
            createCompanyForm.reportValidity();
          } else {
            alert(msg.ieErrorForm);
          }
        } else {
          if(document.getElementById('rootPassword').value == document.getElementById('confirmPassword').value){
            createCompany()
          }else{
            // display passwords do not match
            console.log('passwords are not matching')
          }
        }
      }

      async function createCompany(){
        console.log('createCompany function triggered')
        const companyName = document.getElementById('companyName').value
        const rootUserName = document.getElementById('rootUserName').value
        const rootUserEmail = document.getElementById('rootUserEmail').value
        const rootPassword = document.getElementById('rootPassword').value
        
        const response = await fetch('/createCompany',{
          method: 'POST',
          headers: {
            'Content-type': 'application/json'
          },
          body: JSON.stringify({
            companyName: companyName,
            rootUserName: rootUserName,
            rootUserEmail: rootUserEmail,
            rootUserPassword: rootPassword
          })
        })

        if(response.status == 200){
          document.getElementById('createCompanySuccessMessage').innerHTML = 'Company created!'
          document.getElementById('createCompanySuccessDivision').style.visibility = 'visible'
        }else{
          document.getElementById('createCompanySuccessMessage').innerHTML = 'Error creating company. Try Again!'
          document.getElementById('createCompanySuccessDivision').style.visibility = 'visible'
        }
      }

      function logout() {
        console.log('logging out')
        localStorage.removeItem('token');
        document.getElementsByClassName('logoutButton')[0].submit();
      }
    </script>
  </body>
</html>
